// import CoachDetailScreen from '@/이전 파일들/features/coach/screen/CoachDetail';

const CoachDetailPage = ({ params }: { params: { slug: string } }) => {
  // const { slug } = params;
  // return <CoachDetailScreen id={slug} />;
  return <div>CoachDetailPage</div>;
};

export default CoachDetailPage;
