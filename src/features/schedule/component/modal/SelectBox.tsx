type Props = {
  type: string;
}

const ScheduleModalSelectBox = ({ }: Props) => {
  return(
    <div>
      <select>
        <option>1</option>
        <option>2</option>
      </select>
    </div>
  )
}

export default ScheduleModalSelectBox