const CustomerList = () => {
  return (
    <div css={{ width: '65%' }}>
      <div css={{
        height: '3.75rem',
        padding: '0 0 16px 0',
        fontSize: '1.25rem',
        fontWeight: 600,
        borderBottom: '1px solid var(--grey100)',
      }}>회원 목록</div>
    </div>
  );
};

export default CustomerList;